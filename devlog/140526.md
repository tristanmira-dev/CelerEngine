# What was achieved
- Done the generic image wrapper (contains both image and imageview), and move glfw polling to window class
# Frictions
- Some problems with ownership (raii image and non-raii image)
# Things to try in the next session
- render draw function