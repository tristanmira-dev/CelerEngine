# What was achieved/learned
- Mostly done the image wrapper class and the command buffer wrapper classes
# Frictions
- Harder than i thought, was contemplating so much about how to make these wrapper classes (mix of thinking about performance, good architecture practices etc)
- Holy luckily i found the bug with the context early on, so i was init-ing the context really early on which resulted in its members not being properly initialized (nullptr) 
  and when i actually used one of the members (the queue index) the program crashed, since then changed it such that all context initialization/assignment is done last
# Things to try in the next session
- Integrate these two classes AND actually do the draw function (command buffer, sync object, frames in flight etc)
