# First devlog in a while, oh boy

# What was achieved/learned
- Lots have happened and undocumented stuff as well, lets just say an allocator for buffers kind of works, but needs some more fine tuning since the goal of actually being able to upload vertices during runtime was not achieved (need to design how im gonna use sync objects better, between the mem manager and the renderer)
- working on textures, sub allocator seems to work for images? need to test cuz i used a placeholder image to make the large buffer (dont know if the mem requirements would be the same)
# Frictions
- Of course, another rabbit hole, so the acquire of the buffer and images isnt going to work (the if statement in renderer.cpp), have another design in mind though
# Things to try in the next session
- descriptors for textures
- acquire barriers
- release barriers
- a list that tracks pending acquires so the renderer can consume this? maybe it'd make runtime uploads easier or possible now, check out variants, or just good ol polymorphism